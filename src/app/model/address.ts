export class Address {ç

    street: string;
    portal: number;
    number: number;

    constructor(street: string, portal: number, piso: number) {
        this.street = street;
        this.portal = portal;
        this.number = piso;
    }
}